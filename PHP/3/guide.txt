STEP 1: Use SAME database

USE student_db;

STEP 2: Create Table

CREATE TABLE books (
    book_id INT AUTO_INCREMENT PRIMARY KEY,
    book_name VARCHAR(100),
    author VARCHAR(100),
    price INT
);

STEP 3: Run Project

1. Put folder in htdocs
2. Start Apache + MySQL
3. Open:
   http://localhost/library-ajax/index.php

IMPORTANT:
- Using AJAX (no page reload)
- jQuery used for simplicity
- Same DB as student_db